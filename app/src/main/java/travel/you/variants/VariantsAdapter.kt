package travel.you.variants

import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import travel.you.R

/*
class VariantsAdapter(val variants: List<Varinat>) : RecyclerView.Adapter<VariantsAdapter.VariantsViewHolder>() {

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): VariantsViewHolder {
        val view = LayoutInflater.from(viewGroup.context)
            .inflate(R.layout.text_row_item, viewGroup, false)

        return
    }

    override fun onBindViewHolder(holder: VariantsViewHolder, position: Int) {
        holder.bind()
    }

    override fun getItemCount(): Int {
        return variants.size()
    }

    class VariantsViewHolder(view) :
        RecyclerView.ViewHolder() {
    }

}*/
